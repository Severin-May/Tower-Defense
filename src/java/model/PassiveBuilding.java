package model;

import javax.swing.*;

public abstract class PassiveBuilding extends Building{
    public PassiveBuilding(int x, int y, ImageIcon image, Player owner) {
        super(x, y, image, owner);
    }
}
